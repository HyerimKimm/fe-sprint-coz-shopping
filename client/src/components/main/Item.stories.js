import React from 'react';
import { Item } from './Item';

export default {
    title: "components/Item",
    component: Item
}

const Template = (args) => <Item></Item>

export const ItemTemplate = Template.bind({});

