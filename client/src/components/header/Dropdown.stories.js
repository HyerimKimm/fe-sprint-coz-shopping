import React from 'react';
import { Dropdown } from './Dropdown';

export default {
    title: "components/Dropdown",
    component: Dropdown
}

const Template = (args) => <Dropdown></Dropdown>

export const DropdownTemplate = Template.bind({});

