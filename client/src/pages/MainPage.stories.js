import React from 'react';
import { MainPage } from './MainPage';

export default {
    title: "Pages/MainPage",
    component: MainPage
}

const Template = (args) => <MainPage></MainPage>

export const MainPageTemplate = Template.bind({});

