import React from 'react';
import { BookmarkPage } from './BookmarkPage';

export default {
    title: "Pages/BookmarkPage",
    component: BookmarkPage
}

const Template = (args) => <BookmarkPage></BookmarkPage>

export const BookmarkPageTemplate = Template.bind({});

